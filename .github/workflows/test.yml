name: TEST
on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - run: echo 'int main(void) { puts("HELLO"); }' > test.c

      - uses: ./

      - run: |
          command -v cc
          realpath cc
          cc test.c
